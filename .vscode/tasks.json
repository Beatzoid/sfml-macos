{
    "tasks": [
        {
            "type": "shell",
            "label": "Build",
            "command": "${workspaceFolder}/build.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": ["$gcc"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}
